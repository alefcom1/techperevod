# Решения по 4 измерениям + PDF-примеры переводов — план

Дата: июль 2026. Две связанные задачи: (1) реорганизация навигации и разделов
по образцу RushTranslate — группировка «решений» по языку / документу /
отрасли / сценарию использования; (2) блок «Примеры перевода» с PDF-образцами
на отраслевых и языковых страницах.

---

## Задача 1. Четыре измерения решений

### Текущее состояние → целевое

| Измерение | Было | Стало / план |
|---|---|---|
| По документу | `/uslugi` (12 стр.) — уже фактически «by document» | Пункт меню «Документы» (переименован из «Услуги») |
| По отрасли | `/otrasli` (12 стр.) | Пункт меню «Отрасли» (как было) |
| По языку | `/perevod` (6 стр.) прятался внутри «Услуг» | Отдельный пункт меню «Языки»; расширение до 12+ страниц — волной агентов |
| По сценарию | НЕ БЫЛО | Новый раздел `/zadachi` (hub + [slug]), пункт меню «Задачи» |

Меню верхнего уровня: Продукт · Документы · Языки · Отрасли · Задачи ·
Тарифы · Компания. Одно измерение = один пункт с dropdown — вместо
мега-панели (наш NavBar прост, мобильные аккордеоны уже работают; SEO-ссылки
в dropdown работают на перелинковку).

### Раздел «Задачи» (use cases) — архитектура

- Данные: `src/data/usecases.ts` (interface UseCase: slug, name, heroTitle,
  metaTitle, heroSubtitle, intro[], painPoints[], deliverables[], steps?,
  faq[], relatedServiceSlugs, relatedIndustrySlugs).
- Маршруты: `/zadachi` (hub) + `/zadachi/[slug]` через `UseCasePageBody`
  (по образцу ServicePageBody: JSON-LD Breadcrumb+FAQ, intro, блоки,
  перелинковка, CTA #zakaz).
- Прототип (пишу сам): `sertifikaciya-v-eaes`, `eksport-dokumentacii`.
- Волна агентов (по готовому шаблону, тезисы в ТЗ):
  1. `patentovanie-za-rubezhom` — патентование за рубежом (PCT/EPO);
  2. `zapusk-kitajskogo-oborudovaniya` — запуск китайской линии;
  3. `mezhdunarodnyj-tender` — участие в международном тендере;
  4. `registraciya-medizdelij` — регистрация медизделий/фармы.
  Позже: локализация продукта, шеф-монтаж с иностранным подрядчиком,
  ВЭД-контракты, обучение персонала иностранной технике.

### Расширение языков (отдельная волна агентов, после /zadachi)

По образцу `languages.ts` добавить: итальянский, польский, чешский,
нидерландский, турецкий, корейский, японский, арабский (+хаб обновить).
Каждая страница — тот же состав (intro, linguisticNotes, FAQ, metaTitle).

---

## Задача 2. Блок «Примеры перевода» с PDF

### Что делаем

На каждой отраслевой и языковой странице — блок из 4 карточек PDF-примеров
(«документ этой отрасли → на 4 разных языка» / «документ на языке страницы»).
Языки распределяем по страницам детерминированно-разнообразно (seed от slug),
из пула: en, de, fr, es, it, pl, cs, nl, tr, pt, fi, sv.

### PDF-образцы — генерация

- **Статически, скриптом в репо** (`scripts/generate-samples.mjs`,
  запуск вручную при изменении данных, вывод в `public/samples/*.pdf`) —
  никакой runtime-генерации, ноль нагрузки на прод.
- Библиотека: `pdfkit` (Node, dev-dependency) + TTF-шрифт с кириллицей
  (DejaVu Sans из системы или вкоммиченный в repo `assets/fonts`).
- Контент образцов: `src/data/sampleDocs.ts` — для каждого документа-типа
  фрагмент оригинала (ru) + переводы на языки пула (готовим переводы
  агентами, проверяю выборочно я/носители).
- Каждый PDF:
  1. **Стр. 1** — «перевод»: шапка бюро (Техперевод.com + контакты),
     заголовок типа документа, двуязычная таблица или текст перевода,
     диагональный водяной знак «ПРИМЕР / SAMPLE» (полупрозрачный, поверх);
  2. **Стр. 2** — сертификат точности перевода **на языке перевода**
     (по образцу RushTranslate): заявление о точности, описание документа
     и языковой пары, ФИО/подпись представителя бюро, фирменный бланк;
     водяной знак «ПРИМЕР» тоже ставим (это образец, не боевой сертификат).
- Имена файлов: `public/samples/{docType}-{src}-{tgt}.pdf`
  (напр. `pasport-bezopasnosti-ru-de.pdf`).

### Блок на страницах

- Компонент `SampleTranslationsSection` (карточки: тип документа, пара
  языков, превью-иконка, ссылка на PDF target=_blank).
- Данные соответствия: в `sampleDocs.ts` — маппинг отрасль→типы документов,
  язык→типы документов; выбор 4 штук — детерминированный (hash slug).
- JSON-LD не нужен; alt/aria — обязательно.

### Этапы

1. Каркас: pdfkit-скрипт + 1 пилотный PDF (SDS ru→de) — согласовать вид.
2. Контент образцов для 6–8 типов документов × 4–6 языков — волна агентов
   (перевод фрагментов), моя проверка.
3. Генерация всех PDF, компонент блока, врезка на otrasli/perevod страницы.
4. Деплой + переобход в Вебмастере.

### Правовая заметка

Все образцы — вымышленные данные (без реальных имён/компаний/номеров),
водяной знак «ПРИМЕР» на каждой странице, сертификат — от имени нашего же
бюро (не имитация третьих лиц). Это витрина качества, аналог примеров на
/primery-perevodov, но в «боевом» оформлении PDF.

---

## Этап 2 (июль 2026): языки 14 → 30, примеры → 16+

### Новые 16 языковых страниц (волны агентов + моя вычитка)

Выбор по реальному спросу техперевода в РФ (ЕАЭС, ВЭД, промышленность):

| Волна | Слаги |
|---|---|
| A | s-portugalskogo (авиация/агро Бразилии), s-finskogo (энергетика, лесопром), s-shvedskogo (SKF/Atlas Copco-класс), s-vengerskogo (автопром) |
| B | s-rumynskogo (автокомпоненты), s-bolgarskogo, s-serbskogo, s-ukrainskogo |
| C | s-kazahskogo (ЕАЭС, добыча), s-uzbekskogo (растущий рынок), s-azerbajdzhanskogo (нефтегаз), s-armyanskogo (ЕАЭС) |
| D | s-ivrita (медтех/IT), s-hindi (фарма/IT), s-vetnamskogo (производство), na-kitajskij (экспорт в КНР — обратное направление) |

Каждая страница — полный состав LanguagePage (intro, linguisticNotes с
ложными друзьями и нац. стандартами, typicalDocuments, whoOrdersThis, FAQ,
metaTitle ≤60) + автоматически блок «Примеры перевода», sitemap, JSON-LD.
Меню «Языки» — оставить 8 топ-пунктов + «Все языки → /perevod» (30 в
dropdown не влезает — полный список на хабе).

### PDF-примеры: пул 8 → 16

Принцип качества: витринные PDF генерируются только на языках, где перевод
фрагмента можно гарантировать без носителя (en, de, fr, es, it, pl, cs, nl,
pt). Для новых «экзотических» языков (ko, ja, ar, tr, kk, he, hi, vi …)
PDF-образцы НЕ публикуем, пока фрагменты не вычитаны переводчиками бюро —
черновики можно сгенерировать тем же скриптом позже (этап 3, после проверки
вашими носителями). Разнообразие блока обеспечивается типами документов:

+8 новых PDF (новые ТИПЫ документов, надёжные языки):
protokol-ispytanij-ru-de, obosnovanie-bezopasnosti-ru-en,
instrukciya-montazh-ru-it, etiketka-markirovka-ru-fr,
kommercheskoe-predlozhenie-ru-en, katalog-produkcii-ru-pt,
pasport-izdeliya-ru-pl, shema-elektricheskaya-ru-cs.

pickSamples работает с любым размером пула (hash-выбор 4 из 16).

**Статус: этап 2 выполнен (июль 2026).** Все 16 языковых страниц (волны
A–D) опубликованы — в languages.ts 30 записей; PDF-пул расширен до 16
(включая ru-pt с сертификатом на португальском). Вычитка агентских
текстов проведена: исправлены выдуманные «ложные друзья» (pt/es, sr, uk),
ошибочные названия документов (sv — датские формы, bg, sr, uk), даты
реформ письменности (kk, uz), чтение 万 «вань» и иероглиф 亿 (zh).
PDF на «экзотических» языках — этап 3, после вычитки носителями бюро.
