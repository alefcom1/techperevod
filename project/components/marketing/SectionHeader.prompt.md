Standard section intro used above every content block on the landing page.

```jsx
<SectionHeader title="Четыре шага до готового перевода" />
<SectionHeader
  align="left"
  title="Единственная платформа с доступом к обоим контурам моделей"
  subtitle="GPT, Claude, DeepL и YandexGPT, DeepSeek — легально в одном рабочем процессе."
/>
```

`align="center"` for most sections; `align="left"` for the AI-orchestrator section where the diagram sits alongside/below a left-aligned intro.
