Radial hero illustration — a central document ringed by the model nodes, connected lines carry an animated pulse. This is the primary hero visual described in the brief ("узлы моделей, соединённые линиями к центральному документу, бирюзовые импульсы по линиям").

```jsx
<div style={{width: 420, height: 420}}>
  <HeroModelDiagram />
</div>
```

Square aspect ratio; scales via viewBox. Shares color coding and model list shape with `OrchestratorDiagram` — keep the two in sync if you change the model roster.
