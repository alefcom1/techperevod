Grayscale client-logo wall for social proof.

```jsx
<LogoStrip
  caption="Пилотные клиенты платформы (иллюстративные названия)"
  logos={["ПРОММАШ", "НЕФТЕСЕРВИС", "МЕДТЕХГРУПП", "ИТ-ВЕКТОР", "ГАЗТЕХНО"]}
/>
```

Always keep the `caption` disclosing placeholder status until real client names/logos are supplied — never present invented names as real customers without it.
