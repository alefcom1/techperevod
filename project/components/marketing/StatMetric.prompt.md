Big-number statistic for the social-proof row.

```jsx
<StatMetric value="40 млн+" label="слов переведено" />
<StatMetric value="35%" label="экономии по TM" />
<StatMetric value="2 мин" label="среднее время оценки" />
```

Value is display-format (Manrope, large); render 3–4 in a row with even gaps. Treat these as illustrative placeholders until real figures are supplied — see README caveats.
